#
# indexes.conf - lab indexes, scoped PER LOCATION for RBAC.
#
# RBAC rule: an index belongs to exactly ONE location. Roles grant access
# with a location wildcard (london_*, berlin_*), so a user can never see
# another location's data. See apply.sh (ensure_role) for the matching roles.
#
# Naming: <location>_<datatype>.
#   London = Location 2 (198.18.2.0/24)
#   Berlin = Location 3 (198.18.3.0/24)
#   loc1   = the Splunk server itself (infrastructure; global-only)
#

# ===========================================================================
# loc1 - 198.18.1.0/24  (Splunk server; infrastructure)
# ===========================================================================
[loc1_linux]
homePath   = $SPLUNK_DB/loc1_linux/db
coldPath   = $SPLUNK_DB/loc1_linux/colddb
thawedPath = $SPLUNK_DB/loc1_linux/thaweddb
maxTotalDataSizeMB = 5120
frozenTimePeriodInSecs = 604800

# Host metrics (CPU/mem/disk/load) for the Splunk host itself. Event index:
# key=value events charted with timechart (reliable + directly searchable).
[loc1_metrics]
homePath   = $SPLUNK_DB/loc1_metrics/db
coldPath   = $SPLUNK_DB/loc1_metrics/colddb
thawedPath = $SPLUNK_DB/loc1_metrics/thaweddb
maxTotalDataSizeMB = 2048
frozenTimePeriodInSecs = 604800

# ===========================================================================
# London - Location 2 - 198.18.2.0/24  (ubuntu-loc2, windows-server-2022-loc2, cisco-iq-link)
# ===========================================================================
[london_linux]
homePath   = $SPLUNK_DB/london_linux/db
coldPath   = $SPLUNK_DB/london_linux/colddb
thawedPath = $SPLUNK_DB/london_linux/thaweddb
maxTotalDataSizeMB = 5120
frozenTimePeriodInSecs = 604800

[london_windows]
homePath   = $SPLUNK_DB/london_windows/db
coldPath   = $SPLUNK_DB/london_windows/colddb
thawedPath = $SPLUNK_DB/london_windows/thaweddb
maxTotalDataSizeMB = 5120
frozenTimePeriodInSecs = 604800

[london_network]
homePath   = $SPLUNK_DB/london_network/db
coldPath   = $SPLUNK_DB/london_network/colddb
thawedPath = $SPLUNK_DB/london_network/thaweddb
maxTotalDataSizeMB = 5120
frozenTimePeriodInSecs = 604800

# London host metrics (UF on ubuntu-london). Event index (key=value + timechart).
[london_metrics]
homePath   = $SPLUNK_DB/london_metrics/db
coldPath   = $SPLUNK_DB/london_metrics/colddb
thawedPath = $SPLUNK_DB/london_metrics/thaweddb
maxTotalDataSizeMB = 2048
frozenTimePeriodInSecs = 604800

# ===========================================================================
# Berlin - Location 3 - 198.18.3.0/24  (proxmox-9.2-loc3, ubuntu-loc3)
# ===========================================================================
[berlin_linux]
homePath   = $SPLUNK_DB/berlin_linux/db
coldPath   = $SPLUNK_DB/berlin_linux/colddb
thawedPath = $SPLUNK_DB/berlin_linux/thaweddb
maxTotalDataSizeMB = 5120
frozenTimePeriodInSecs = 604800

[berlin_proxmox]
homePath   = $SPLUNK_DB/berlin_proxmox/db
coldPath   = $SPLUNK_DB/berlin_proxmox/colddb
thawedPath = $SPLUNK_DB/berlin_proxmox/thaweddb
maxTotalDataSizeMB = 5120
frozenTimePeriodInSecs = 604800

[berlin_network]
homePath   = $SPLUNK_DB/berlin_network/db
coldPath   = $SPLUNK_DB/berlin_network/colddb
thawedPath = $SPLUNK_DB/berlin_network/thaweddb
maxTotalDataSizeMB = 5120
frozenTimePeriodInSecs = 604800

# Berlin host + Proxmox resource metrics (UF on ubuntu-berlin). Event index;
# holds both linux:metrics (ubuntu-berlin) and proxmox:metrics (key=value).
[berlin_metrics]
homePath   = $SPLUNK_DB/berlin_metrics/db
coldPath   = $SPLUNK_DB/berlin_metrics/colddb
thawedPath = $SPLUNK_DB/berlin_metrics/thaweddb
maxTotalDataSizeMB = 2048
frozenTimePeriodInSecs = 604800

# Web-app container: HTTP access log (webapp:access) + reachability probe
# (webapp:probe from ubuntu-berlin). Berlin asset -> covered by berlin_* RBAC.
[berlin_web]
homePath   = $SPLUNK_DB/berlin_web/db
coldPath   = $SPLUNK_DB/berlin_web/colddb
thawedPath = $SPLUNK_DB/berlin_web/thaweddb
maxTotalDataSizeMB = 2048
frozenTimePeriodInSecs = 604800

# PostgreSQL container (db-berlin) - Postgres logs + the demo app's DB writes.
[berlin_db]
homePath   = $SPLUNK_DB/berlin_db/db
coldPath   = $SPLUNK_DB/berlin_db/colddb
thawedPath = $SPLUNK_DB/berlin_db/thaweddb
maxTotalDataSizeMB = 2048
frozenTimePeriodInSecs = 604800
