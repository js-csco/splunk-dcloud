#
# commands.conf - registers the labsync custom search command.
# labsync runs bin/labsync.sh (git snapshot -> push to lab-snapshot branch)
# and returns a status row. Invoked by the "Save to GitHub" dashboard.
#

[labsync]
filename = labsync.py
chunked = false
python.version = python3
