#
# transforms.conf - KV Store lookup for the Save-to-GitHub request queue.
#
[git_save_requests_lk]
external_type = kvstore
collection = git_save_requests
fields_list = _key, branch, message, status, result, commit, requested_by, requested_at, updated_at
