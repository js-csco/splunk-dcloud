#
# app.conf - metrics: host & infrastructure metrics (log-to-metrics).
#
[install]
is_configured = true
state = enabled

[ui]
is_visible = true
label = Host & Infra Metrics

[launcher]
author = js-csco
description = Host CPU/mem/disk/load and Proxmox resource metrics (metric indexes).
version = 0.1.0

[package]
id = metrics
