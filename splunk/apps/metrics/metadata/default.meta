# metrics - globally visible, readable by all (RBAC still applies per index).

[]
access = read : [ * ], write : [ admin, power ]
export = system

[views]
export = system

[nav]
export = system

# Make the metric sourcetype parsing global so it applies to data no matter
# which app/context indexes it.
[props]
export = system
