#
# collections.conf - KV Store collections for the Asset Configuration inventory,
# one PER LOCATION so access can be enforced per role (RBAC), matching the
# index-per-location model. Seeded from lookups/assets_seed.csv by apply.sh.
#
[dcloud_assets_loc1]
enforceTypes = false

[dcloud_assets_london]
enforceTypes = false

[dcloud_assets_berlin]
enforceTypes = false
