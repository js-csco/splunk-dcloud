#
# app.conf - Correlation app manifest
#

[install]
is_configured = true
state = enabled

[ui]
is_visible = true
label = Correlation

[launcher]
author = js-csco
description = Demonstrates correlating activity across two or three data sources by a shared key (service, user, host) and time.
version = 0.1.0

[package]
id = correlation
