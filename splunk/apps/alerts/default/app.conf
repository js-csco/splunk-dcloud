#
# app.conf - alerts: metric-threshold + forwarder-health alerts (email).
#
[install]
is_configured = true
state = enabled

[ui]
is_visible = true
label = Alerts

[launcher]
author = js-csco
description = CPU/memory threshold and "forwarder stopped sending" alerts (email + Triggered Alerts).
version = 0.1.0

[package]
id = alerts
