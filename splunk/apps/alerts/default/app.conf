#
# app.conf - alerts: metric-threshold + forwarder-health alerts (email).
#
[install]
is_configured = true
state = enabled

[ui]
is_visible = true
label = Alerts

[launcher]
author = js-csco
description = CPU/memory, forwarder-health, SNMP-trap and service-health alerts (email, Triggered Alerts, and a custom Webex message action).
version = 0.1.0

[package]
id = alerts
