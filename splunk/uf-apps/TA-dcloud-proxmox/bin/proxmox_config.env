# Proxmox connection for poll_proxmox.py (DEMO lab creds - not secret).
# Override at runtime with $SPLUNK_HOME/var/lib/dcloud/proxmox.env or env vars.
# Auth: if PROXMOX_TOKEN is set it is used; otherwise PROXMOX_USER + PROXMOX_PASSWORD
# (ticket auth).
PROXMOX_HOST=198.18.3.170
PROXMOX_PORT=8006
PROXMOX_USER=root@pam
PROXMOX_PASSWORD=cisco
